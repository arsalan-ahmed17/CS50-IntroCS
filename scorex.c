#include <cs50.h>
#include <stdio.h>

const int N = 3;

float average(int length, int array[]);
{
    int scores[N]
    for(int i = 0; i < 3 ; i++)
    {
        scores[i] = get_int(Score: ")
    }
}

float average(int length, int array[])
{

}

